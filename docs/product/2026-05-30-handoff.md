# Codex Claude Handoff - 2026-05-30

## Current State

- Branch: `main`
- HEAD: `206794c0953e82dba022d1651574ab8bb03f026b`
- Main worktree status at initial handoff creation: clean
- `git worktree list` shows only `/Users/anyi/codex-claude`
- `.claude/worktrees` exists and is empty
- Historical job/run metadata remains under `.codex-claude-delegate/jobs` and `.codex-claude-delegate/runs`

## Release Update - 2026-05-30

- Published npm package: `@anyi61/codex-claude-delegate-mcp@0.1.11`
- Registry confirmation: `latest` is `0.1.11`
- Registry modified timestamp observed: `2026-05-30T02:38:41.025Z`
- Local version files are synchronized at `0.1.11`:
  - `package.json`
  - `package-lock.json`
  - `plugins/codex-claude-delegate/.codex-plugin/plugin.json`
  - `plugins/codex-claude-delegate/.claude-plugin/plugin.json`

Release-flow fix added before publishing:

- Added `scripts/sync-plugin-version.mjs`.
- Added `npm run sync:plugin-version`.
- Updated `release`, `release:minor`, and `release:major` so `npm version ...` is followed by plugin metadata version synchronization before `npm publish`.
- Added `tests/sync-plugin-version.test.ts` for version sync behavior and release-script ordering.

Verification for the release-flow fix:

- `npx vitest run tests/sync-plugin-version.test.ts tests/check-release-assets.test.ts`
- `npm run typecheck`
- `npm run check:release`
- `npm run audit:docs`
- `npm run sync:plugin-version`
- `git diff --check`

Post-publish repository steps remaining:

- Commit the `0.1.11` release bump and release-flow fix.
- Create and push tag `v0.1.11`.

## Completed Goal Sequence

The latest completed Goal sequence was:

1. `REL-002`
2. `AUDIT-DOCS-002`
3. `STATE-MACHINE-001`

Each item followed the agreed loop:

- Claude produced a plan first.
- Codex reviewed the plan and requested changes where needed.
- Claude implemented in delegated worktrees.
- Codex reviewed the diff and verification output.
- Fix iterations were requested until acceptance criteria were met.
- Accepted work was applied to the main workspace.
- Targeted verification was run.
- Commits were created.
- `docs/product/2026-05-20-comprehensive-review.md` was updated with completion notes and commit hashes.
- Delegated worktrees were cleaned.

## Recent Commits

- `ecbe5dd` `test: add release install smoke check`
- `432ba77` `docs: record rel-002 completion`
- `e9a71c8` `test: add source-derived docs audit checks`
- `d7c6f11` `docs: record audit-docs-002 completion`
- `4b6185b` `test: add state machine fault injection coverage`
- `206794c` `docs: record state-machine-001 completion`

Nearby earlier commits relevant to the broader hardening push:

- `10fcc88` `test: add main workspace apply collision matrix`
- `6826085` `feat: add environment config phase 2`
- `2c773ce` `feat: add artifact index metadata`
- `a880277` `fix: stabilize release doctor smoke tests`
- `14cbd32` `feat: add multi-root read review context`
- `c0ff2f3` `docs: record FUNC-010 completion`

## REL-002

Purpose: verify that the npm tarball can be installed and that packaged entry points are runnable after installation.

Key changes:

- Added `scripts/check-release-install-smoke.mjs`.
- Added `check:release:install` to `package.json`.
- Added `check:release:install` to `prepublishOnly` after `check:release`.
- Added `tests/check-release-install-smoke.test.ts`.
- The smoke script uses `npm pack`, installs the generated `.tgz` into a temp project, then verifies package metadata, bin entry, `dist/cli.js`, and `dist/server.js`.

Important review decision:

- The implementation reads package name/version/bin from `package.json` and parses `npm pack --json` output instead of hardcoding release metadata.

Verification run during the task:

- `npx vitest run tests/check-release-install-smoke.test.ts tests/check-release-assets.test.ts`
- `npm run typecheck`
- `npm run build`
- `npm run check:release`
- `npm run audit:docs`
- `npm run check:release:install`

## AUDIT-DOCS-002

Purpose: strengthen docs drift detection by deriving audit expectations from source/plugin metadata instead of relying only on fixed text checks.

Key changes:

- Updated `scripts/audit-docs.mjs`.
- Updated `tests/audit-docs.test.ts`.
- Added source-derived checks for:
  - default tool count/list
  - unknown backticked `claude_*` tool names
  - `claude_apply` metadata contradictions
  - `preview_token` documentation/schema consistency
  - plugin/package version consistency

Important review decision:

- A Claude attempt that hardcoded tool lists was rejected.
- The accepted design parses runtime source/plugin/package data at audit time.
- Missing README advanced tools remains informational.

Verification run during the task:

- `npx vitest run tests/audit-docs.test.ts`
- `npm run audit:docs`
- `npm run typecheck`

Known informational output:

- `README.md: informational - missing advanced tools: claude_export`

## STATE-MACHINE-001

Purpose: add fault-injection coverage for background jobs, job store, review gate, dedupe, and recovery boundaries.

Key changes:

- Added tests across:
  - `tests/background-jobs.test.ts`
  - `tests/jobs.test.ts`
  - `tests/review-gate.test.ts`
- Production source changes were not required for this item.

Covered scenarios include:

- terminal/crashed jobs are not reused by dedupe
- spawn failure handling
- mid-wait state transitions
- recovery grace windows
- dead PID recovery
- active job cleanup preservation
- update-missing-record behavior
- review gate disabled no-op behavior
- review gate fingerprint matching
- pending gate overwrite behavior
- corrupt/deleted review gate state file fail-closed behavior

Important review decision:

- One brittle assertion was corrected: in the test harness, the replacement dedupe job can quickly become `failed`; the test asserts separate job creation and no dedupe reuse, not a specific final status.

Verification run during the task:

- `npx vitest run tests/background-jobs.test.ts tests/jobs.test.ts tests/review-gate.test.ts`
- `npm run typecheck`
- `npm run audit:docs`

## Worktree Observation

During the latest Goal sequence, multiple delegated worktrees appeared for the same high-level item because each Claude implement/fix iteration creates a distinct worktree named from the run id:

- `codex-delegated-<runId-prefix>`

Observed pattern:

- `REL-002`: initial implement plus review-fix implement
- `AUDIT-DOCS-002`: initial implement plus review-fix implement
- `STATE-MACHINE-001`: initial implement plus review-fix implement

This is expected with the current run model. Physical worktrees were cleaned at the end; historical job/run records are retained for audit.

Recommended follow-up:

- `UX/RUN-GROUP-001`: add `goal_item_id`, `parent_run_id`, or `supersedes_run_id` to delegated run metadata so `claude_result`, `claude_workspace_status`, and cleanup output can group repeated attempts under one high-level task.

## Current Verification Baseline

Latest targeted checks from the completed sequence passed:

- release metadata tests
- release install smoke tests
- release asset tests
- docs audit tests
- background job/job store/review gate state-machine tests
- `npm run typecheck`
- `npm run audit:docs`

The final status check before this handoff showed a clean main worktree and no delegated worktree residue.

## Suggested Next Priorities

1. `STATE-MACHINE-002`
   - Extend fault injection around partial artifact writes, process start time/PID reuse metadata, and review gate invalidation after delegated worktree mutation.
   - Status: completed after this handoff in commit pending at time of edit. Added targeted fault-injection coverage for artifact index tmp/corrupt/stale-entry behavior, background job recovery PID/heartbeat edge cases, and review-gate fingerprint binding. Also tightened review-gate clearing so empty-string binding fields are treated as explicit mismatches instead of being ignored.

2. `AUDIT-DOCS-003`
   - Decide whether informational missing advanced-tool docs, currently including `claude_export`, should remain informational or become a release-blocking audit.
   - Status: completed. Promoted from informational to release-blocking. `claude_export` added to README advanced tools section.

3. `REL-003`
   - Add post-pack or post-install metadata checks for registry/tag alignment if release workflow continues to happen manually.
   - Status: completed. Added `scripts/check-release-metadata.mjs` with npm registry version/latest and `git tag --points-at HEAD` alignment checks. Wired as `check:release:metadata` script (not in prepublishOnly or release scripts). Test fixtures available via `CHECK_RELEASE_NPM_VIEW_JSON`, `CHECK_RELEASE_GIT_TAGS`, `CHECK_RELEASE_SKIP_NPM`, and `CHECK_RELEASE_SKIP_GIT`.

4. `MULTI-ROOT-002`
   - Add more provenance-focused tests and docs around context root citations, especially for review output.
   - Status: completed. Prompt/docs/test hardening only; no runtime validation change.

Completed after this handoff:

- `UX/RUN-GROUP-001`: added optional caller-provided `goal_item_id` / `supersedes_run_id` grouping metadata across write jobs, run logs, `claude_runs`, `claude_result`, and `claude_workspace_status`. This groups repeated implement/fix attempts without changing dedupe fingerprints, worktree naming, cleanup, or apply safety behavior.

- `STATE-MACHINE-002`: added fault-injection coverage for artifact index fallback behavior, background crash recovery, and review gate fingerprint bindings. Verification: `npx vitest run tests/artifact-index.test.ts tests/background-jobs.test.ts tests/review-gate.test.ts`, `npm run typecheck`, `npm run audit:docs`, and `git diff --check`.

## AUDIT-DOCS-003

Purpose: promote the informational missing advanced-tool docs check to a release-blocking audit failure, and document `claude_export` in the README.

Key changes:

- Updated `scripts/audit-docs.mjs`: changed the missing advanced tools check from informational `console.log` to a blocking `failures.push`.
- Updated `tests/audit-docs.test.ts`: added tests for missing and complete advanced tool documentation; updated fixtures to include `claude_export` in `BASE_TOOL_DEFINITIONS` and `validReadme`.
- Updated `README.md`: added `claude_export` to the advanced tools TOML list and table.

Verification:

- `npx vitest run tests/audit-docs.test.ts`
- `npm run audit:docs`
- `npm run typecheck`

## REL-003

Purpose: add post-publish metadata checks for registry and git tag alignment with local package.json version.

Key changes:

- Added `scripts/check-release-metadata.mjs`.
- Added `check:release:metadata` to `package.json` scripts (not added to `prepublishOnly` or release scripts).
- Added `tests/check-release-metadata.test.ts`.
- Updated `README.md` with metadata check usage in the release checklist.

The script reads `package.json` for package name and version, then verifies:

- The npm registry `dist-tags.latest` matches the local version.
- The git tag `v{version}` exists locally.

Both checks support fixture and local dry-run environment variables: `CHECK_RELEASE_NPM_VIEW_JSON`, `CHECK_RELEASE_GIT_TAGS`, `CHECK_RELEASE_SKIP_NPM`, and `CHECK_RELEASE_SKIP_GIT`.

Current repository note: `npm run check:release:metadata` verifies registry `0.1.12` successfully, then fails because `v0.1.12` points at the release commit while current `HEAD` contains later development commits. This is the intended post-publish check behavior; use it after creating/pushing the next release tag.

Verification:

- `npx vitest run tests/check-release-metadata.test.ts`
- `CHECK_RELEASE_SKIP_GIT=1 npm run check:release:metadata`
- `npm run audit:docs`
- `npm run typecheck`

## MULTI-ROOT-002

Purpose: strengthen context-root provenance guidance so query and review outputs cite additional repositories by alias and path.

Key changes:

- Updated `src/schema.ts`: `buildContextRootsPromptSection` now includes a citation rule requiring `[alias]` attribution when findings, claims, or answers are based on context-root content.
- Updated `tests/schema.test.ts`: added tests for provenance text in `buildContextRootsPromptSection`, `buildReviewPrompt` with context roots, `buildQueryPrompt` with context roots, and absence when context roots are not provided.
- Updated `README.md`: expanded both the feature bullet and the detailed `context_roots` note to mention alias/path citations.

Important:

- No runtime validation, permission allowlist, or JSON schema changes.
- Prompt/docs/test hardening only.

Verification:

- `npx vitest run tests/schema.test.ts`
- `npm run typecheck`
- `npm run audit:docs`
- `git diff --check`

## Operating Notes

- Keep using Goal mode for multi-item work:
  - one item at a time
  - Claude plan first
  - Codex review before implementation
  - Claude implement/self-test
  - Codex diff review and targeted verification
  - apply only after acceptance
  - one item, one implementation commit plus docs commit when commit hash recording is needed
  - cleanup delegated worktrees after each item
- Do not edit `dist/` manually.
- Prefer targeted tests plus `typecheck`/`audit:docs`.
- Run full `npm test` when changes touch core apply/security behavior or when targeted evidence leaves uncertainty.
- `docs/product/2026-05-20-comprehensive-review.md` may need `git add -f` if docs ignore rules hide it.
